{
  "name": "prithvinet",
  "version": "1.0.0",
  "private": true,
  "description": "Smart Environmental Monitoring & Compliance Platform for Air, Water & Noise",
  "scripts": {
    "dev": "concurrently -n client,server,ml -c blue,green,yellow \"npm run dev --prefix client\" \"npm run dev --prefix server\" \"cd ml-service && uvicorn app.main:app --reload --port 8000\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "lint": "npm run lint --prefix client && npm run lint --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
